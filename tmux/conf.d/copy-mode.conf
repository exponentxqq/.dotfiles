# ============================================================================
# Copy Mode 配置 — vim 风格键位
# ============================================================================

# 进入复制模式
bind Enter copy-mode

# -------------------------------------------------------------------
# 选择和退出
# -------------------------------------------------------------------
run -b 'tmux bind -T copy-mode-vi v send -X begin-selection 2> /dev/null || true'
run -b 'tmux bind -T copy-mode-vi V send -X select-line 2> /dev/null || true'
run -b 'tmux bind -T copy-mode-vi C-v send -X rectangle-toggle 2> /dev/null || true'
run -b 'tmux bind -T copy-mode-vi y send -X copy-selection-and-cancel 2> /dev/null || true'
run -b 'tmux bind -T copy-mode-vi Enter send -X copy-selection 2> /dev/null || true'
run -b 'tmux bind -T copy-mode-vi Escape send -X cancel 2> /dev/null || true'
run -b 'tmux bind -T copy-mode-vi q send -X cancel 2> /dev/null || true'

# -------------------------------------------------------------------
# 光标移动（h/j/k/l/w/b/e/0/$ 为 mode-keys vi 内置，此处仅列出需显式绑定的键位）
# -------------------------------------------------------------------
# 注：以下键位在 mode-keys vi 下为 tmux 内置默认行为，无需显式绑定：
#   h/j/k/l — 方向移动
#   w/b/e   — 跳词
#   0/^/$   — 行首/行首非空/行尾
#   gg/G    — 历史顶部/底部（gg 按两次 g，G 单次）
#   C-u/C-d — 上半页/下半页
#   / / ?   — 向前/向后搜索
#   n/N     — 下一个/上一个搜索匹配

# -------------------------------------------------------------------
# 系统剪贴板集成
# -------------------------------------------------------------------
if -b 'command -v xsel > /dev/null 2>&1' 'bind y run -b "tmux save-buffer - | xsel -i -b"'
if -b '! command -v xsel > /dev/null 2>&1 && command -v xclip > /dev/null 2>&1' 'bind y run -b "tmux save-buffer - | xclip -i -selection clipboard >/dev/null 2>&1"'

# -------------------------------------------------------------------
# Buffer 管理
# -------------------------------------------------------------------
bind b list-buffers
bind p paste-buffer
bind P choose-buffer
